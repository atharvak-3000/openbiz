{
  "name": "openbiz-assignment",
  "version": "1.0.0",
  "description": "Full-stack Openbiz assignment with scraper, backend, and frontend",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install-all"
  },
  "keywords": ["openbiz", "fullstack", "react", "express", "puppeteer"],
  "author": "Openbiz Assignment",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
